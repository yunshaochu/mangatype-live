# 调查报告 V2：AI 多组断句/字号候选方案的更务实设计

## 一句话结论

- 这个想法仍然值得做，但 V1 应该**收缩目标**：先做“**第 0 组自然译文 + 第 1~N 组排版候选 + 快速切换**”，**暂时不做前端测量/自动优选**。
- 原因不是测量不重要，而是当前项目**没有可信的可用区域材料**：红框偏小、bubble 几何又有明显误差，硬测只会制造伪精确。
- 因此，V1 的核心不是“自动判断哪组最好”，而是“**让 AI 多给几组排版思路，用户能低成本切换**”。

---

## 与 V1 报告相比，方向上的修正

V1 里我提出过“AI 候选 + 前端 fit 评分 + 默认优选”的方向。这个方向长期看仍然成立，但经过进一步澄清后，当前版本需要做两个重要修正：

### 修正 1：第 0 组不是 `breakAfter` 候选

第 0 组是**特殊组**。

- 它是 AI 输出的“**断句最自然的译文**”
- 它本身已经携带换行
- 它不是由 `breakAfter` 生成的
- 它应该被当成“主译文 / 主展示结果”

也就是说：

- **第 0 组 = AI 直接给出的自然断句版文本**
- **第 1 组开始 = 额外排版候选**

### 修正 2：V1 暂时不做前端测量

这个修正非常关键。

原因如下：

- 红框的范围对中文排版来说通常**偏小**，它更适合覆盖日文原文，而不是约束中文最终排版
- AI 生成的 bubble 几何也存在明显误差，不能作为高可信边界
- 在“红框不准 + bubble 也不准”的前提下，本地测量即便做出来，也会因为输入边界不可信而不可信

因此当前阶段更合理的选择是：

> **先做候选切换，不做自动测量，不做自动优选。**

这不是否认测量的价值，而是承认当前缺乏可靠基础数据。

### V1 边界与兼容契约（冻结）

为避免后续实现边界漂移，V1 先冻结下面这组契约：

- **第 0 组永远是主结果**：它就是 AI 直接输出的自然断句译文，渲染、编辑、保存、导出都先以它为默认生效结果。
- **第 1~N 组只承载排版候选**：候选仅允许表达 `breakAfter + fontSize`，不引入新的主文本语义。
- **V1 不做三件事**：不做前端候选测量、不做自动优选、也不要求 AI 直接输出 `baseText`。
- **新增字段必须保持 optional**：旧 bubble / 旧工程里只有 `text`、`fontSize` 等历史字段时，导入、渲染、保存都必须继续工作。
- **`extraLayoutVariantCount` 缺失或为 `0` 时完全按旧格式回退**：此时系统只保留第 0 组主结果，不新增任何必填字段，也不要求存在候选数组。

可以把它理解成下面两个兼容档位：

```json
// legacy bubble（V1 必须继续兼容）
{
  "text": "旧格式译文",
  "fontSize": 1.2
}

// V1 bubble（第 0 组主结果 + 可选候选）
{
  "text": "主结果",
  "fontSize": 1.2,
  "baseText": "主结果去断行后的标准化文本",
  "activeLayoutIndex": 0,
  "layoutVariants": [
    { "text": "主结果", "fontSize": 1.2 },
    { "breakAfter": [6, 12], "fontSize": 1.1 }
  ]
}
```

上面的示例是**兼容语义说明**，不是要求一次性把所有能力都实现完成；V1 仍然遵守“先打通候选切换，后续再考虑测量/优选”的节奏。

---

## 当前共识下的产品目标

### V1 要解决的问题

V1 不去解决“AI 几何识别是否绝对准确”，而是只解决这件事：

> 同一个 bubble 已经大致放对位置，但字号偶尔过大、断句偶尔不顺，用户不想手动一点点微调。

因此 V1 的用户体验目标应当是：

- AI 先给一份自然译文（第 0 组）
- 再给若干组备用排版（第 1~N 组）
- 用户不满意时，可以一键切换到下一组
- 不要求系统自动判断哪组最优

---

## 候选组的正确语义

### 第 0 组：自然译文主结果

第 0 组应该具备这些特征：

- AI 直接输出完整译文
- 文本本身携带换行
- 语义最自然
- 作为默认显示结果

举例：

```json
{
  "text": "今天晚上\n一起走吧",
  "fontSize": 1.3
}
```

这里的换行不是算法插进去的，而是 AI 自己判断“这样最自然”。

### 第 1~N 组：额外排版候选

从第 1 组开始，候选不必重写整句译文。

更好的做法是：

- 共享同一句文本内容
- 只改变断句位置
- 允许附带字号变化

也就是说，额外候选的职责是：

- **排版变化**，不是**语义变化**

这比“每组都重写一遍整句译文”更稳，也更省 token。

---

## 候选数应该让用户可配置

这是一个很合理的交互增强。

但需要强调一个口径：

- 用户配置的不是“总组数”
- 用户配置的是“**额外候选数**”

也就是：

- `0` = 只有第 0 组
- `1` = 第 0 组 + 1 组额外候选
- `2` = 第 0 组 + 2 组额外候选

我建议：

- 默认值：`1` 或 `2`
- 上限：`3`

原因：

- 候选太少，切换价值不明显
- 候选太多，返回更慢、协议更脆、用户也不愿意一个个翻

### 推荐配置字段

建议新增一个全局配置项，例如：

```ts
extraLayoutVariantCount?: number;
```

语义是：

> 除第 0 组自然译文外，额外请求 AI 返回多少组布局候选。

---

## 是否需要 `plainText` / `baseText`

答案是：**需要一个“无断句基准文本”，但 V1 不建议让 AI 输出它。**

### 为什么需要它

因为第 1~N 组如果只给 `breakAfter`，前端必须有一份“原始连续文本”作为断句基准。

否则没法从：

- `breakAfter = [2, 5]`

稳定重建出：

- `今天\n晚上一起\n走吧`

### 让 AI 输出 `plainText` 合不合理？

我现在的判断是：**没必要，甚至不太划算。**

原因：

- AI 已经输出了第 0 组 `text`
- 再让它输出一份无换行文本，收益不大
- 还会增加 schema 复杂度和冗余

所以 V1 更好的做法是：

> **前端从第 0 组 `text` 派生出一份 `baseText`，并存储下来。**

### 但不能粗暴地“直接删换行”

这里你的直觉是对的：

- 简单把 `text` 里的 `\n` 清掉，确实有点粗糙

不是完全不能这么做，而是不能**只**这么做。

更合理的本地派生步骤应该是：

1. 统一换行符
2. 按行切开
3. 去掉每行首尾空白
4. 再重新拼接
5. 若前后是英文/数字片段，必要时补空格

所以结论是：

- `baseText` 不建议由 AI 输出
- `baseText` 可以本地派生
- 但这一步要作为一个**正式的标准化过程**，不能只是随手 `replace(/\n/g, '')`

### 命名建议

我建议别叫 `plainText`，叫：

- `baseText`
- 或 `layoutBaseText`

会更准确。

因为它不是“纯文本”的哲学概念，而是“排版候选的基准文本”。

---

## 推荐的数据结构（V1 版）

### 顶层 bubble 字段建议

```ts
type LayoutVariant = {
  breakAfter: number[];
  fontSize?: number;
};

type Bubble = {
  text: string;
  baseText?: string;
  fontSize: number;
  layoutVariants?: LayoutVariant[];
  activeLayoutIndex?: number;
};
```

### 字段含义

- `text`
  - 第 0 组自然译文
  - 自带换行
- `baseText`
  - 从 `text` 本地派生出的连续文本
  - 作为第 1~N 组 `breakAfter` 的基准
- `fontSize`
  - 第 0 组字号
- `layoutVariants`
  - 第 1~N 组额外候选
- `activeLayoutIndex`
  - 当前显示第几组
  - `0` 表示显示 `text`
  - `1` 表示显示 `layoutVariants[0]`

### 一个完整示例

```json
{
  "text": "今天晚上\n一起走吧",
  "baseText": "今天晚上一起走吧",
  "fontSize": 1.3,
  "layoutVariants": [
    {
      "breakAfter": [4],
      "fontSize": 1.2
    },
    {
      "breakAfter": [2, 5],
      "fontSize": 1.1
    }
  ],
  "activeLayoutIndex": 0
}
```

含义：

- `0` 组：直接显示 `text`
- `1` 组：根据 `baseText + [4]` 生成断句
- `2` 组：根据 `baseText + [2,5]` 生成断句

---

## `breakAfter` 的定义建议

### 推荐口径：1-based，表示“第几个字后断开”

例如：

- `breakAfter: [2, 5]`

表示：

- 在第 2 个字后换行
- 在第 5 个字后再换行

这个表达比 0-based 更贴近提示词和人工理解。

### 计数基准

`breakAfter` 必须基于：

- `baseText`

而不是基于：

- 第 0 组 `text`
- 或当前显示文本

否则断点在不同候选之间无法稳定复用。

### 一个实现上的注意点

如果后续要做到更稳，计数最好基于“可见字符单元（grapheme）”，而不是粗暴的 JS 字符下标。

不过这件事可以放到实现时再做，不影响当前协议讨论。

---

## AI 输出协议建议

### AI 应该输出什么

AI 输出内容建议分成两层：

1. 第 0 组自然译文
2. 若干组额外排版候选

示意：

```json
{
  "text": "今天晚上\n一起走吧",
  "fontSize": 1.3,
  "layoutVariants": [
    { "breakAfter": [4], "fontSize": 1.2 },
    { "breakAfter": [2, 5], "fontSize": 1.1 }
  ]
}
```

### AI 不应该输出什么

V1 不建议让 AI 额外输出：

- `baseText`
- 候选测量结果
- 候选适配评分
- 每组完整重写版译文

这些都会拉高复杂度，但不明显提高当前阶段的可用性。

---

## 为什么不建议“每组都重写整句译文”

这个点在当前版本里已经比较明确了。

### 不推荐的原因

- 冗余：同一句话被反复输出多次
- 漂移：候选 2、候选 3 可能偷偷改变措辞，导致切换时不只是排版变化
- 更耗 token
- 更难校对
- 更难解释给用户：到底是在切排版，还是在切翻译？

### 推荐的原则

V1 应该尽量把这两件事分开：

- **译文**：主结果负责
- **排版**：候选负责

---

## 前端暂时不测量之后，系统能力边界是什么

这件事需要说清楚，避免预期过高。

### V1 能解决的

- 同一句中文，给用户多个断句/字号方案
- 用户一键切换
- 少做很多手动改字号、删换行、重排的工作

### V1 暂时不能解决的

- 自动判断哪个候选最不溢出
- 自动判定真实气泡可用区
- bubble 几何本身就明显错误时的自动修正

换句话说：

> V1 解决的是“排版试错成本”，不是“几何识别精度”。

---

## 用户手动编辑后的处理规则

这里建议尽早定规则，不然后面状态会很乱。

### 推荐规则

如果用户手动修改了：

- 第 0 组 `text`

那么应该：

- 重新派生 `baseText`
- 并清空 `layoutVariants`

或者至少：

- 标记现有候选“已失效”

因为原来的 `breakAfter` 是基于旧文本的，继续沿用很容易错位。

---

## 推荐的 V1 落地顺序

### 第一步：数据结构扩展

目标：支持第 0 组 + 第 1~N 组候选。

建议会涉及：

- `types.ts`
- `hooks/useProcessor.ts`

### 第二步：AI prompt / schema 扩展

目标：让 AI 能返回：

- 第 0 组自然译文
- 若干额外布局候选

建议会涉及：

- `services/geminiService.ts`
- `services/translationPromptPresets.ts`

### 第三步：前端切换交互

目标：

- 显示当前候选编号
- 提供“上一组 / 下一组”操作
- 支持快捷键切换

建议会涉及：

- `components/BubbleEditor.tsx`
- `components/BubbleLayer.tsx`
- `App.tsx`

### 第四步：用户可配置额外候选数

目标：

- 在设置中允许用户指定额外候选数量

建议会涉及：

- `types.ts`
- `contexts/ProjectContext.tsx`
- 设置面板相关组件

---

## 最终建议

基于当前讨论，我认为最合理的 V1 方案是：

### 做

- 第 0 组自然译文
- 第 1~N 组 `breakAfter + fontSize` 候选
- 用户可配置额外候选数
- 快速切换交互
- 本地派生并保存 `baseText`

### 不做

- 前端候选测量
- 自动优选
- 让 AI 输出 `baseText`
- 每组候选都重写一遍完整译文

---

## 当前版本的总判断

如果用一句话概括当前共识，那就是：

> **先把“译文”和“排版”拆开，再把“排版候选切换”做出来；至于自动测量，等以后有更可信的几何材料再说。**

这是一个更务实、也更适合这个项目现状的方案。
