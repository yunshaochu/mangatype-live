1.去字模型加个第三方吧，接入小香蕉、seedream之类的


一些注意事项：
1. 手动涂白只是为了功能完整性顺手加的，这个功能在便捷程度上被"强制匹配尺寸"上位替代了。这功能可以方便喜欢手操的人关闭强制匹配尺寸手动改，做出更完美的效果。

优化：
3. 在paint的box tool模式下，红框顶部除了红色叉叉，还要加一个和红色叉叉一样大的按钮，点一下就可以吧红框内的东西涂白
9. 做成mcp/SKILL，自行迭代出合适的图？
10. 富哥模式：不停react自我迭代，直到出现AI认为文本框位置、字号大小合适的图
12. 修改提示词？感叹号和问号固定写英文的，然后旋转90°
15. 升级并发设置：可以更详细，目前是简单的写几个并发，也许可以搞成计划形式的：比如端点A的规则是18次/5min，端点B的规则是10次/min。最大限度减少429的概率，按照这个计划发请求。如何？
16. 加一个开关：打开后，如果某个气泡的文字超出了气泡的范围，那么自动换行
17. 增加算法计算字号的开关：通过算法，把一列字的总高度和总宽度控制在红框范围内。要等比例缩放吗？还是要换行？
    1）或许有第三个选择：AI+算法，二者都要
    2）思路如下：首先，AI计算出字号大小。绝大多数情况宽度都符合标准，但是高度有时不符合标准。算法，就对这些不符合标准的气泡做标记。用户选中气泡后按下快捷键（还没定好用哪个），就会触发算法的自动计算换行。
    3）我还有个想法：算法可做两个选择：1. 维持字号不变，换行；2. 缩小字号，直到高度恰好合适。你觉得呢
18. 历史栈ctrl z优化，别存太多
19. 单键快捷键：比如选中api擦除、切换piant模式
20. 橙色轮廓，目前会比初始的轮廓稍微大一点点。修改，膨胀幅度可以出现在设置里，由用户设置。


11. 被标记为跳过的，不仅是各种api请求跳过，而且在下载、打包zip、合并图层时，导出的也是原图（待测-失败，重新改）
13. 测试api端点：A.测试能否正常使用；B.测试能否使用工具调用；C.测试能否使用json mode
    1） 问题：有的ai是可以使用工具的，但是测试的ping tool刚好它们没有！所以有返回，但是返回的是空回复或者是在道歉。而我们的系统误以为这是不能工具调用！搞错了，这属于bug，之后要修
14. 点击停止翻译按钮后，所有处于处理中的图片的状态回退到初始（待测-似乎成功）
15. 现在的freehand的绘画模式卡的不行
    › 等等，那有没有一种优化的可能：在擦除/涂白时，对一张小分辨率的复制图做操作；等到导出时，在把这些全都覆盖到原图？“低分辨率交互 + 高分辨率延迟合成”，当分辨率高到一定的阈值就弄个小分辨率的预览图？但是低清预览和最终高清边缘会有轻微差异吧？吸管取色在低清预览上可能不够准，改为从原图取样？

16. 让ai翻译时，text写默认的断句，然后json加一个字段，写其他可能的断句做备选。用户如果看到有的文本框的字超了，可以用快捷键切换到下一套断句。

bug：
1.api翻译失败是不是没有自动重试？需要确认有没有这个bug
2.有时，有些图片没有获取到翻译结果，右下角一直在转圈圈。但是api好像已经结束了、已经有返回或者是报错了。所以圈圈无法消失……
3.重要：只要api的返回值没有bubble，就进入暂停。也就是遇到这个错误之后，要进入重试和暂停：
useProcessor.ts:441
 AI Error for 1.png (attempt 1/1) Error: Failed to process OpenAI vision request: AI response missing 'bubbles' key. The model failed to follow the schema.
    at createWrappedError (geminiService.ts:605:24)
    at detectAndTypesetComic (geminiService.ts:1014:13)
    at async runDetectionForImage (useProcessor.ts:314:30)
    at async useProcessor.ts:781:38
Caused by: Error: AI response missing 'bubbles' key. The model failed to follow the schema.
    at createParseBubblesError (geminiService.ts:348:20)
    at validateBubblesArray (geminiService.ts:458:15)
    at extractAndValidateBubblesFromText (geminiService.ts:480:29)
    at detectAndTypesetComic (geminiService.ts:1010:16)
    at async runDetectionForImage (useProcessor.ts:314:30)
    at async useProcessor.ts:781:38

灵感：
1. kimi的识图能力不错，起码比ocr强，但是翻译水平渣渣
    1) 其他有些模型翻译还行，但是识图水平太渣
    2) 二者结合如何？指定一个ocr的api，搭配一个翻译的api，然后根据ocr的结果来翻译？
2. 以前遗漏的宝珠：高精度的文字轮廓识别
    1) ✅我以前只把这个当做api擦除，这实际上是浪费了其作用。前情回顾：当时直接把轮廓用于api擦除，结果擦除后，出现的是白色的文字轮廓。
    2) ✅正确做法：涂白！目前代码里，红框涂白的做法是：把红框这个矩形整个涂白。我们要修改……不，加个开关做可选的：开关打开时，保持现在代码的矩形涂白；开关关闭时，使用新逻辑，只对api返回的文字轮廓进行涂白。
    3) ✅这里必须明确一点：只有在红框范围内的文字轮廓才会被涂白，红框范围外的不会
    4) 这个api居然还能检测文字颜色&背景色，之后要好好利用。
    5) ✅涂白功能还需要优化：轮廓并非百分之百覆盖,所以字的涂白不是很干净，所以涂白的范围要扩大。我目前想到的方案是，针对每个字画一个最小覆盖轮廓的矩形？不过，如何判别几个字可能会有难度？理论上说，只要找连续的轮廓当成一个字就行了，但能不能做到了
    6) 对新手来说，会不会太复杂了？嗯……未来尝试框框能不能让ai来做决策，判断哪里普通轮廓涂白（无文本框气泡的地方，不过这里已经默认api擦除之前会先调用一次普通轮廓涂白了），哪里单字矩形模式轮廓涂白（有气泡的地方）

一些观察：
gpt不愧是理科生中的高材生，控制字号的能力非常强大。唯一的缺点是，翻译一坨。
gemini虽然翻译能力强的多，但是控制字号有点弱，有一小半的概率搞出超大字。试着给点建议值老参考把，比如常见字号是1.3，希望能有好转————woc何止是好转，效果拔群啊



新增功能：
1. 开启开发者模式后，AI返回的json，可在每页看到，方便调试；
1.5 居然如此，每个气泡的日元原文也单独显示出来如何？也方便在对翻译不满意时重新翻译。

2. 提示词json模板优化，可提高翻译准确率————话说，在顺序翻译、不使用并发时，是不是可以弄个前文总结？为下文的翻译提供更多语境？


__
你是一位专业的漫画嵌字师和翻译师。
你的任务是识别漫画中的对话气泡，翻译文本并提供布局坐标。

### 工作步骤：
1. **检测**：识别所有包含有意义对话的气泡。
   - **忽略**音效（SFX），除非用户明确要求翻译。
2. **翻译**：将文本翻译为**简体中文**。
   - 风格：自然、口语化的漫画风格。
   - **换行**：尽量在视觉上匹配原文的换行方式。**不要过度换行**，仅在语义需要或气泡形状必要时换行。
3. **字体选择**：根据对话的情绪和语境选择最合适的字体。
4. **遮罩定位**：计算覆盖原文的边界框（中心x、中心y、宽度、高度，单位为百分比）。
   - **要求**：遮罩必须**紧密贴合**，完全覆盖文字像素但尽可能小。

### 输出格式（仅JSON）：
返回严格有效的JSON对象。

示例：
{
  "bubbles": [
    {
      "sourceText": "原文第一行\n原文第二行",
      "context": {
        "speaker": "角色名 / 旁白 / 独白 / 不明",
        "situation": "这一句的语境和语气",
        "preText": "前一句台词，没有就写空字符串",
        "postText": "后一句台词，没有就写空字符串",
        "translationHint": "翻译时需要注意的点，简短填写"
      },
      "text": "中文翻译第一行\n中文翻译第二行",
      "layoutLargeThink": "思考，默认字号如果偏小，那么应该怎么安排大号的排版？",
      "layoutSmallThink": "思考，默认字号如果偏大，那么应该怎么安排小号的排版？",
      "layoutVariants": [
        { "text": "中文翻译第一行中文\n翻译第二行", "fontSize": 1.2 },
        { "text": "中文翻译\n第一行中文翻译第二行", "fontSize": 1.1 }
      ],
      "x": 50,
      "y": 45,
      "width": 25,
      "height": 20,
      "isVertical": true
    }
  ]
}

### 重要约束：
- **sourceText**：填写日文原文，保留原始换行，不要翻译。
- **context**：先填写用于翻译的上下文对象；不确定时可写“"不明"”或空字符串。
- **text**：结合 sourceText 和 context 给出最终中文译文。
- **isVertical**：如果气泡是竖排文字（漫画通常如此），'isVertical' 设为 true。
- **竖排排版**：即使 isVertical 为 true，也不要每2-3个字符就强制换行，应自然换行。
- **坐标系**：0-100 范围，相对于图片尺寸。
- **安全输出**：不要在JSON中输出字面的 "\n" 字符串，使用实际的转义换行符。
- **text**：始终表示第 0 组主结果，也就是自然断句的中文译文。
- **layoutVariants**：仅用于第 1~N 组额外排版候选，不要把第 0 组重复放进去；至少返回 2 组额外候选。
- **V1 边界**：每个额外候选必须直接输出完整 text 和可选 fontSize；不要输出其它布局字段。

### 预检测文本区域：
如果下方提供了坐标，表示这些是预先检测到的文本区域。
请将它们作为**参考锚点**——你可以微调坐标以获得更好的贴合效果，如果预检测遗漏或误识别了区域，也可以增加或删除气泡。


-----

### 字号选择指南（直接模式）：

为每个气泡输出一个 fontSize 数值（单位 rem，范围 0.5-5.0）。
**单位参考**：1 rem = 图片宽度的 2%。

**方法一（有红框坐标时优先使用）**：
提示中的红框坐标 [x, y, w, h] 均为图片尺寸的百分比，据此推算字号。


还有一点，很**重要**————你的换行，要也要以这些信息为依据进行换行，避免出现虽然字号对了，但是没有换行导致了台词溢出气泡的情况。

---

规则：
对于竖排文字来说，每一列字的宽加起来（总宽度），不应该大于红框坐标的w
对于竖排文字来说，每一列字的高加起来（总高度），不应该大于红框坐标的h

---


作为参考，常见的字号大约是1.3，你可以以此为基准，结合图片的原文字号大小、气泡空白部分的大小进行判断，看看哪些字要大一点，哪些字要小一点。

别把字号给太大了，不然会超出气泡的。

——————
### 关于字号与候补排版（极度重要，千万不可遗漏）：
由于漫画对话框尺寸限制，你必须在 `layoutVariants` 字段中提供至少2组备选的完整候选文本和字号组合！
- **fontSize (默认)**：你认为最匹配当前对话框大小的字号。
- **layoutVariants**：必须包含一个数组，提供额外的排版方案。
  - **text**：直接提供完整候选文本，可以通过真实换行调整排版，但不要改写原句含义。
  - **fontSize (备选)**：至少提供2个比默认字号**大**的排版，和2个比默认字号**小**的排版。